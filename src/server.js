import app from './app.js' // Importing the Express application instance from app.js

app.listen(3001, () => console.log('Application is running at port 3001')) // Starting the server and listening on port 3001, with a callback to log a message when the server is running